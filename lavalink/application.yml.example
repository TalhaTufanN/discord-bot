server:
  port: 2333
  # SADECE localhost. Disari acilmiyor; botlar ayni makinede.
  address: 127.0.0.1

lavalink:
  plugins:
    - dependency: "dev.lavalink.youtube:youtube-plugin:1.18.1"
      repository: "https://maven.lavalink.dev/releases"
      snapshot: false
    # NOT: LavaSrc (Spotify) BILEREK yok. Album yuklerken parca detaylari icin
    # /v1/tracks?ids= (multi-get) cagiriyor; Spotify bu endpoint'i 2024-11-27
    # sonrasi acilan app'lere 403 ile kapatti -> album hic cozulemiyordu.
    # Spotify artik bot tarafinda cozuluyor: utils/spotify.js — bize multi-get
    # gerekmiyor, /albums/{id}/tracks yeterli.
  server:
    password: "${LAVALINK_PASSWORD}"   # gercek deger VPS-teki application.yml-de
    sources:
      # Dahili youtube KAPALI olmali; youtube-source plugin'i devraliyor.
      youtube: false
      bandcamp: false
      soundcloud: true
      twitch: false
      vimeo: false
      nico: false
      http: true   # radyo istasyonlari (stations.js) icin
      local: true  # yerel Sagopa dosyalari icin
    filters:
      volume: true
      equalizer: true
    bufferDurationMs: 400
    frameBufferDurationMs: 5000
    opusEncodingQuality: 10
    resamplingQuality: LOW
    trackStuckThresholdMs: 10000
    useSeekGhosting: true
    youtubePlaylistLoadLimit: 6
    playerUpdateInterval: 5
    youtubeSearchEnabled: true
    soundcloudSearchEnabled: true

plugins:
  youtube:
    enabled: true
    allowSearch: true
    allowDirectVideoIds: true
    allowDirectPlaylistIds: true
    # Datacenter IP icin sira onemli: ANDROID_VR PO-token istemiyor,
    # MUSIC de genelde datacenter'dan gecebiliyor.
    clients:
      - MUSIC
      - ANDROID_VR
      - WEB
      - WEBEMBEDDED

metrics:
  prometheus:
    enabled: false

logging:
  level:
    root: INFO
    lavalink: INFO
